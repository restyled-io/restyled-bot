{
  "icon_emoji": ":cloud:",
  "channel": "${channel}",
  "username": "cloudgenesis",
  "attachments": [
    {
      "fallback": "Stack <${link}|${stack}> has entered status: ${status}",
      "title": "Stack <${link}|${stack}> has entered status: ${status}",
      "text": "${StackId}",
      "color": "${good|warning|danger}"
    }
  ]
}
